{
  "name": "@sniprl/api",
  "version": "1.0.0",
  "private": true,
  "description": "Fastify HTTP API server and background worker for SnipRL",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "db:migrate": "echo \"Prisma migration will be configured in Phase 1 Step 5\""
  },
  "dependencies": {
    "@sniprl/shared": "workspace:*",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
